#ifndef	MAIN_H
# define MAIN_H

# include <unistd.h>
# include <stdlib.h>
# include <stdarg.h>
int		_printf(const char *format, ...);
char	*_strchr(char *s, char c);
int		_strlen(char *s);
void	_puts(char *str);

#endif